base-path:/api/v1
paths:
    /demo/login:
      post:
        caller-application:
           -microservice-name
   /getUser/{userId}:
        get:
       caller-application:
           -telesales-web
nginx-config.yaml file should be present on root directory within service code.
**************************************

